apply plugin: 'java'

version = '1.0'

jar {
    manifest {
        attributes 'Implementation-Title': 'sanselanandroid', 'Implementation-Version': version
    }
}

dependencies {
    sourceCompatibility = 1.7
    targetCompatibility = 1.7
}

